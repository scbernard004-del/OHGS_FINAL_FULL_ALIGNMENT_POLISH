Upload only these files to the same GitHub repository. Do not upload images, assets, or videos again. This patch fixes the mobile header alignment, balances logo/menu/theme/language/WhatsApp in one line, improves desktop/web spacing, and shrinks the header smoothly when scrolling.